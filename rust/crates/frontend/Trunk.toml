[build]
target = "index.html"
dist = "dist"

[watch]
watch = ["src", "index.html", "styles"]

[serve]
address = "127.0.0.1"
port = 8080

[[proxy]]
rewrite = "/api"
backend = "http://localhost:3000/api"

[[proxy]]
rewrite = "/ws"
backend = "ws://localhost:3000/ws"
ws = true
